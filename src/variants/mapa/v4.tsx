"use client";
// Variante 4 de "Arma tu recorrido" — pendiente de generar (placeholder:
// re-exporta la sección actual para que el registro compile siempre).
import MapaSection from "@/sections/MapaSection";
export default MapaSection;
